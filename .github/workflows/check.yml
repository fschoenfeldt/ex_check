name: check

on: [pull_request, push]

jobs:
  mix_check:
    name: mix check (Elixir ${{matrix.elixir}}, OTP ${{matrix.otp}})

    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - elixir: '1.20.2'
            otp: '28.5'
            os: 'ubuntu-24.04'
          - elixir: '1.16.1'
            otp: '26.2.2'
            os: 'ubuntu-22.04'
          - elixir: '1.14.5'
            otp: '24.3.4.16'
            os: 'ubuntu-22.04'
          # Elixir 1.12 floor: OTP 22 has no prebuilt for any live runner
          # (only the retired ubuntu-20.04), so test 1.12 on OTP 24 instead.
          - elixir: '1.12.3'
            otp: '24.3.4.16'
            os: 'ubuntu-22.04'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}

      - name: Cache deps and build
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: deps-${{ runner.os }}-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}-git-${{ github.sha }}
          restore-keys: |
            deps-${{ runner.os }}-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('**/mix.lock') }}
            deps-${{ runner.os }}-${{ matrix.otp }}-${{ matrix.elixir }}

      - name: Fetch deps
        run: mix deps.get

      - name: Remove application build
        run: mix clean

      - name: Compile deps
        run: mix deps.compile

      - name: Compile app
        run: mix compile

      - name: Run check
        run: mix check
